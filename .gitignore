# Build output
build/
*.elf
*.nro
*.nacp
*.map
*.lst
*.o
*.d

# Local testing config — contains a live romm_host + api_key.
# Never commit this. Copy to config.json yourself when testing locally.
config.json

# Python venv used by the dev-time asset/doc scripts
venv/
__pycache__/

# Scratch/debug artifacts left over from development
# (API response dumps, doc scrapes — not used by the Makefile build)
roms_sample.json
platforms_sample.json
roms_test.json
page3.json
plat1.json
plat14.json
plat1_page2.json
openapi.json
raw_docs.md
base_url_docs.txt
test_image.png
NUL

# OS cruft
.DS_Store
Thumbs.db
